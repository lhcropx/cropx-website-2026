{
  "$schema": "https://schemas.wp.org/trunk/block.json",
  "apiVersion": 3,
  "name": "cropx/icon-columns",
  "title": "4-6 Column Crop List with Icons",
  "category": "cropx-text-only",
  "icon": "columns",
  "description": "Header above a 4–6 column, column-major layout (fills column 1 top-to-bottom, then column 2, and so on). JavaScript balances items across columns by count, so the last column never has more items than any other — falls back to a browser-balanced CSS layout if JavaScript is unavailable. Each item has an icon, heading, body, and optional CTA link.",
  "keywords": ["columns", "icons", "features", "masonry", "stacked"],
  "supports": {
    "html": false,
    "align": false,
    "anchor": true
  },
  "attributes": {
    "columnCount": {
      "type": "string",
      "default": "4",
      "enum": ["4", "5", "6"]
    },
    "eyebrow": {
      "type": "string",
      "default": "The CropX platform"
    },
    "heading": {
      "type": "string",
      "default": "One platform for every layer of your operation"
    },
    "backgroundVariant": {
      "type": "string",
      "default": "white",
      "enum": ["taupe", "white", "blue"]
    },
    "segmentAccent": {
      "type": "string",
      "default": "general",
      "enum": ["general", "enterprise", "service-provider", "on-farm"]
    },
    "columns": {
      "type": "array",
      "default": [
        {
          "icon": "fields",
          "heading": "Manage all your farms, fields, and crops in one place",
          "body": "The CropX platform aggregates data from across your entire operation in a single, customisable interface — with farm management tools to organise and schedule activities.",
          "ctaLabel": "See how it works",
          "ctaUrl": "#"
        },
        {
          "icon": "sensor-cloud",
          "heading": "Make timely, data-driven agronomic decisions",
          "body": "Insights and decision-support systems — covering disease risk, irrigation scheduling, and crop management — help your team act with confidence in real time.",
          "ctaLabel": "See how it works",
          "ctaUrl": "#"
        },
        {
          "icon": "antenna",
          "heading": "Connect all your IoT devices, sensors, and farm machines",
          "body": "The CropX system integrates with the equipment you already have — sensors, weather stations, irrigation controllers, and more — into one unified data layer.",
          "ctaLabel": "See how it works",
          "ctaUrl": "#"
        }
      ]
    },
    "eyebrowColor": { "type": "string",  "default": "cropx-blue" },
    "showEyebrow":  { "type": "boolean", "default": true },
    "showHeading":  { "type": "boolean", "default": true },
    "showIcons":    { "type": "boolean", "default": true }
  },
  "editorScript": "file:./index.js",
  "editorStyle":  "file:./index.css",
  "style":        "file:./style-index.css",
  "viewScript":   "file:./view.js",
  "render":       "file:./render.php"
}
